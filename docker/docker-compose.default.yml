services:
  site:
    image: alex2young-github-io_dev
    environment:
      - JEKYLL_ENV
    ports:
      - 4000:4000
    container_name: alex2young-github-io_dev
    volumes:
      - ..:/usr/src/app
    stdin_open: true
    tty: true
    command: bundle exec jekyll serve -H 0.0.0.0 -t
